#include "main.h"
#include <string.h>

/**
 * _strlen - take an word  en return his lenght
 *@s: the word to get the legnt
 *
 * Return: the lenght of the word s
 */

int _strlen(char *s)
{
	return (strlen(s));
}

