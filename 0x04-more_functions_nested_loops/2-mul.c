#include "main.h"

/**
 *mul: - multiply to number and return the result
 *@a: the first number to multiply
 *@b: the second number to multiply
 *
 *Return: this function return the value of a*b
 */
int mul(int a, int b)
{

return (a*b);
}
