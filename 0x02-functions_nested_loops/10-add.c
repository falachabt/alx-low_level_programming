#include "main.h"

/**
 *add: - function take in parameters two intergers and sum
 *@num_1: the first number we want to sum
 *@num_2: the second number to sum 
 *
 *Return: this function return the sum of the two interger pass in parameter
 */
int add(int num_1, int num_2)
{
int result;
result = num_1 + num_2;

return (result);
}
